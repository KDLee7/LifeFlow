
React automatically re-renders a component whenever its state changes.

Instead of manually updating HTML, I update the state.

React compares the new state to the old state and updates the UI automatically.

One piece of state can control multiple elements on the screen.